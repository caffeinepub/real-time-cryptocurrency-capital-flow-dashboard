var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
import { c as createLucideIcon, r as reactExports, j as jsxRuntimeExports, w as Button, x as cn, y as BINANCE_FUTURES_REST_BASE, B as BINANCE_SPOT_REST_BASE, z as useComposedRefs, T as TrendingUp, C as React, D as useInternetIdentity, A as Activity, Q as QueryClientProvider, E as QueryClient, J, F as LoginButton, G as Footer, H as Toaster } from "./index-C5QA2IZY.js";
import { A as Alert, a as AlertDescription } from "./alert-DMbaE6f_.js";
import { C as Card, a as CardHeader, b as CardTitle, c as CardContent, P as Primitive, d as createContextScope, B as Badge, T as Target, S as Separator, e as ChartColumn } from "./index-HSqYiWTT.js";
import { R as RefreshCw } from "./refresh-cw-BADDaFDv.js";
import { T as TrendingDown } from "./trending-down-BQAueTCT.js";
import { Z as Zap } from "./zap-BQJKmpBr.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  ["path", { d: "M10.268 21a2 2 0 0 0 3.464 0", key: "vwvbt9" }],
  [
    "path",
    {
      d: "M17 17H4a1 1 0 0 1-.74-1.673C4.59 13.956 6 12.499 6 8a6 6 0 0 1 .258-1.742",
      key: "178tsu"
    }
  ],
  ["path", { d: "m2 2 20 20", key: "1ooewy" }],
  ["path", { d: "M8.668 3.01A6 6 0 0 1 18 8c0 2.687.77 4.653 1.707 6.05", key: "1hqiys" }]
];
const BellOff = createLucideIcon("bell-off", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [
  ["path", { d: "M10.268 21a2 2 0 0 0 3.464 0", key: "vwvbt9" }],
  [
    "path",
    {
      d: "M3.262 15.326A1 1 0 0 0 4 17h16a1 1 0 0 0 .74-1.673C19.41 13.956 18 12.499 18 8A6 6 0 0 0 6 8c0 4.499-1.411 5.956-2.738 7.326",
      key: "11g9vi"
    }
  ]
];
const Bell = createLucideIcon("bell", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  ["ellipse", { cx: "12", cy: "5", rx: "9", ry: "3", key: "msslwz" }],
  ["path", { d: "M3 5V19A9 3 0 0 0 21 19V5", key: "1wlel7" }],
  ["path", { d: "M3 12A9 3 0 0 0 21 12", key: "mv7ke4" }]
];
const Database = createLucideIcon("database", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  ["rect", { width: "18", height: "11", x: "3", y: "11", rx: "2", ry: "2", key: "1w4ew1" }],
  ["path", { d: "M7 11V7a5 5 0 0 1 10 0v4", key: "fwvmzm" }]
];
const Lock = createLucideIcon("lock", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  [
    "path",
    {
      d: "M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.22.38a2 2 0 0 0 .73 2.73l.15.1a2 2 0 0 1 1 1.72v.51a2 2 0 0 1-1 1.74l-.15.09a2 2 0 0 0-.73 2.73l.22.38a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.22-.39a2 2 0 0 0-.73-2.73l-.15-.08a2 2 0 0 1-1-1.74v-.5a2 2 0 0 1 1-1.74l.15-.09a2 2 0 0 0 .73-2.73l-.22-.38a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z",
      key: "1qme2f"
    }
  ],
  ["circle", { cx: "12", cy: "12", r: "3", key: "1v7zrd" }]
];
const Settings = createLucideIcon("settings", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  [
    "path",
    {
      d: "m21.73 18-8-14a2 2 0 0 0-3.48 0l-8 14A2 2 0 0 0 4 21h16a2 2 0 0 0 1.73-3",
      key: "wmoenq"
    }
  ],
  ["path", { d: "M12 9v4", key: "juzpu7" }],
  ["path", { d: "M12 17h.01", key: "p32p05" }]
];
const TriangleAlert = createLucideIcon("triangle-alert", __iconNode);
const COPY = {
  // Page title and description
  title: "Order Flow Monitor",
  subtitle: "Real-time BTC order analysis",
  // Market types
  marketFutures: "Futures",
  marketSpot: "Spot",
  // States
  loading: "Loading...",
  authRequired: "Authentication Required",
  authMessage: "Log in with Internet Identity to access the Order Flow Monitor.",
  waitingForData: "Waiting for Data",
  waitingMessage: "Enable polling to start receiving order flow data.",
  // Controls
  market: "Market",
  pollingActive: "Polling Active",
  pollingPaused: "Polling Paused",
  lastUpdated: "Last updated",
  settings: "Settings",
  refresh: "Refresh",
  enablePolling: "Enable Polling",
  clearAlerts: "Clear Alerts",
  // Sections
  overview: "Overview",
  flowImbalance: "Flow & Imbalance",
  confluenceEvents: "Confluence Events",
  alerts: "Alerts",
  analysisSettings: "Analysis Settings",
  // Overview metrics
  currentPrice: "Current Price",
  spread: "Spread",
  volume24h: "24h Volume",
  // Flow metrics
  buyFlow: "Buy Flow",
  sellFlow: "Sell Flow",
  netDelta: "Net Delta",
  imbalance: "Imbalance",
  largeOrders: "Large Orders",
  clusterDetected: "Cluster Detected",
  noCluster: "No Cluster",
  // Confluence
  bookDirection: "Book Direction",
  spreadTrend: "Spread Trend",
  noConfluence: "No confluence events detected",
  // Alerts
  noAlerts: "No alerts",
  alertsCount: (count) => `${count} alert${count !== 1 ? "s" : ""}`,
  // Settings labels
  largeTradeThreshold: "Large Trade Threshold (USD)",
  tradeWindow: "Trade Window",
  timeWindow: "Time Window (min)",
  minImbalance: "Min Imbalance (%)",
  minSpreadChange: "Min Spread Change (%)",
  pollingInterval: "Polling Interval (ms)",
  // Error messages
  errorProcessing: "An error occurred while processing order flow data.",
  errorCause: "This may be due to malformed data or API connection issues.",
  tryAgain: "Try Again",
  reloadPage: "Reload Page",
  troubleshooting: "Troubleshooting tips:",
  troubleshootingTips: [
    "Check your internet connection",
    "Try switching between Spot and Futures markets",
    "Clear browser cache and reload the page",
    "If the problem persists, wait a few minutes and try again"
  ]
};
class OrderFlowErrorBoundary extends reactExports.Component {
  constructor(props) {
    super(props);
    __publicField(this, "handleReset", () => {
      this.setState({ hasError: false, error: null });
      if (this.props.onReset) {
        this.props.onReset();
      }
    });
    this.state = { hasError: false, error: null };
  }
  static getDerivedStateFromError(error) {
    return { hasError: true, error };
  }
  componentDidCatch(error, errorInfo) {
    console.error("OrderFlowMonitor error:", error, errorInfo);
  }
  render() {
    if (this.state.hasError) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-6 p-6", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Card, { className: "terminal-panel border-neon-pink/30", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(CardHeader, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs(CardTitle, { className: "text-neon-pink flex items-center gap-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-5 h-5" }),
          "Order Flow Error"
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(CardContent, { className: "space-y-4", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(Alert, { className: "border-neon-pink/30 bg-neon-pink/5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "h-4 w-4 text-neon-pink" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(AlertDescription, { className: "text-sm", children: [
              COPY.errorProcessing,
              " ",
              COPY.errorCause
            ] })
          ] }),
          this.state.error && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-3 rounded-lg bg-muted/50 border border-border", children: /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-mono text-muted-foreground break-all", children: this.state.error.message }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-3", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Button,
              {
                onClick: this.handleReset,
                className: "bg-neon-cyan/20 text-neon-cyan hover:bg-neon-cyan/30 border border-neon-cyan/50",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshCw, { className: "w-4 h-4 mr-2" }),
                  COPY.tryAgain
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              Button,
              {
                onClick: () => window.location.reload(),
                variant: "outline",
                className: "border-neon-yellow/30 text-neon-yellow hover:bg-neon-yellow/10",
                children: COPY.reloadPage
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "pt-4 border-t border-border", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: COPY.troubleshooting }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("ul", { className: "text-xs text-muted-foreground list-disc list-inside space-y-1 mt-2", children: COPY.troubleshootingTips.map((tip) => /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: tip }, tip)) })
          ] })
        ] })
      ] }) });
    }
    return this.props.children;
  }
}
function Input({ className, type, ...props }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "input",
    {
      type,
      "data-slot": "input",
      className: cn(
        "file:text-foreground placeholder:text-muted-foreground selection:bg-primary selection:text-primary-foreground dark:bg-input/30 border-input flex h-9 w-full min-w-0 rounded-md border bg-transparent px-3 py-1 text-base shadow-xs transition-[color,box-shadow] outline-none file:inline-flex file:h-7 file:border-0 file:bg-transparent file:text-sm file:font-medium disabled:pointer-events-none disabled:cursor-not-allowed disabled:opacity-50 md:text-sm",
        "focus-visible:border-ring focus-visible:ring-ring/50 focus-visible:ring-[3px]",
        "aria-invalid:ring-destructive/20 dark:aria-invalid:ring-destructive/40 aria-invalid:border-destructive",
        className
      ),
      ...props
    }
  );
}
var NAME = "Label";
var Label$1 = reactExports.forwardRef((props, forwardedRef) => {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive.label,
    {
      ...props,
      ref: forwardedRef,
      onMouseDown: (event) => {
        var _a;
        const target = event.target;
        if (target.closest("button, input, select, textarea")) return;
        (_a = props.onMouseDown) == null ? void 0 : _a.call(props, event);
        if (!event.defaultPrevented && event.detail > 1) event.preventDefault();
      }
    }
  );
});
Label$1.displayName = NAME;
var Root$2 = Label$1;
function Label({
  className,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Root$2,
    {
      "data-slot": "label",
      className: cn(
        "flex items-center gap-2 text-sm leading-none font-medium select-none group-data-[disabled=true]:pointer-events-none group-data-[disabled=true]:opacity-50 peer-disabled:cursor-not-allowed peer-disabled:opacity-50",
        className
      ),
      ...props
    }
  );
}
async function fetch24hTicker(symbol, market, signal) {
  const base = market === "futures" ? BINANCE_FUTURES_REST_BASE : BINANCE_SPOT_REST_BASE;
  const endpoint = market === "futures" ? "/fapi/v1/ticker/24hr" : "/api/v3/ticker/24hr";
  const url = `${base}${endpoint}?symbol=${symbol}`;
  try {
    const response = await fetch(url, { signal });
    if (!response.ok) {
      console.warn(`Failed to fetch 24h ticker: ${response.status}`);
      return null;
    }
    const data = await response.json();
    if (!data || typeof data.symbol !== "string") {
      console.warn("Invalid 24h ticker response shape");
      return null;
    }
    return data;
  } catch (error) {
    if (error.name !== "AbortError") {
      console.error("Error fetching 24h ticker:", error);
    }
    return null;
  }
}
async function fetchRecentTrades(symbol, market, limit = 100, signal) {
  const base = market === "futures" ? BINANCE_FUTURES_REST_BASE : BINANCE_SPOT_REST_BASE;
  const endpoint = market === "futures" ? "/fapi/v1/trades" : "/api/v3/trades";
  const url = `${base}${endpoint}?symbol=${symbol}&limit=${limit}`;
  try {
    const response = await fetch(url, { signal });
    if (!response.ok) {
      console.warn(`Failed to fetch recent trades: ${response.status}`);
      return [];
    }
    const data = await response.json();
    if (!Array.isArray(data)) {
      console.warn("Invalid recent trades response: expected array");
      return [];
    }
    return data.filter(
      (trade) => trade && typeof trade.price === "string" && typeof trade.qty === "string" && typeof trade.time === "number"
    );
  } catch (error) {
    if (error.name !== "AbortError") {
      console.error("Error fetching recent trades:", error);
    }
    return [];
  }
}
async function fetchBookTicker(symbol, market, signal) {
  const base = market === "futures" ? BINANCE_FUTURES_REST_BASE : BINANCE_SPOT_REST_BASE;
  const endpoint = market === "futures" ? "/fapi/v1/ticker/bookTicker" : "/api/v3/ticker/bookTicker";
  const url = `${base}${endpoint}?symbol=${symbol}`;
  try {
    const response = await fetch(url, { signal });
    if (!response.ok) {
      console.warn(`Failed to fetch book ticker: ${response.status}`);
      return null;
    }
    const data = await response.json();
    if (!data || typeof data.bidPrice !== "string" || typeof data.askPrice !== "string") {
      console.warn("Invalid book ticker response shape");
      return null;
    }
    return data;
  } catch (error) {
    if (error.name !== "AbortError") {
      console.error("Error fetching book ticker:", error);
    }
    return null;
  }
}
function generateOrderFlowFingerprint(data) {
  var _a, _b, _c, _d;
  if (!data) return null;
  const tickerLastPrice = ((_a = data.ticker) == null ? void 0 : _a.lastPrice) || "0";
  const tickerPriceChangePercent = ((_b = data.ticker) == null ? void 0 : _b.priceChangePercent) || "0";
  const bookBid = ((_c = data.bookTicker) == null ? void 0 : _c.bidPrice) || "0";
  const bookAsk = ((_d = data.bookTicker) == null ? void 0 : _d.askPrice) || "0";
  const latestTradeIds = data.recentTrades.slice(0, 5).map((t) => t.id.toString()).join(",");
  return {
    tickerLastPrice,
    tickerPriceChangePercent,
    bookBid,
    bookAsk,
    latestTradeIds
  };
}
function fingerprintsEqual(a, b) {
  if (!a || !b) return false;
  return a.tickerLastPrice === b.tickerLastPrice && a.tickerPriceChangePercent === b.tickerPriceChangePercent && a.bookBid === b.bookBid && a.bookAsk === b.bookAsk && a.latestTradeIds === b.latestTradeIds;
}
const DEFAULT_SYMBOL = "BTCUSDT";
function useBinanceOrderFlow(options) {
  const { market, pollingInterval = 3e3, enabled = true } = options;
  const [data, setData] = reactExports.useState(null);
  const [isLoading, setIsLoading] = reactExports.useState(false);
  const [error, setError] = reactExports.useState(null);
  const [lastUpdated, setLastUpdated] = reactExports.useState(null);
  const abortControllerRef = reactExports.useRef(null);
  const intervalRef = reactExports.useRef(null);
  const lastFingerprintRef = reactExports.useRef(null);
  const isInitialLoadRef = reactExports.useRef(true);
  const isManualRefetchRef = reactExports.useRef(false);
  const fetchData = async (isManualRefetch = false) => {
    if (!enabled) return;
    if (abortControllerRef.current) {
      abortControllerRef.current.abort();
    }
    abortControllerRef.current = new AbortController();
    const signal = abortControllerRef.current.signal;
    if (isInitialLoadRef.current || isManualRefetch) {
      setIsLoading(true);
    }
    if (isInitialLoadRef.current || isManualRefetch) {
      setError(null);
    }
    try {
      const [ticker, trades, book] = await Promise.all([
        fetch24hTicker(DEFAULT_SYMBOL, market, signal),
        fetchRecentTrades(DEFAULT_SYMBOL, market, 100, signal),
        fetchBookTicker(DEFAULT_SYMBOL, market, signal)
      ]);
      if (!ticker && trades.length === 0 && !book) {
        throw new Error("No data available from Binance API");
      }
      const newData = {
        ticker,
        recentTrades: trades,
        bookTicker: book
      };
      const newFingerprint = generateOrderFlowFingerprint(newData);
      if (!fingerprintsEqual(lastFingerprintRef.current, newFingerprint)) {
        setData(newData);
        setLastUpdated(Date.now());
        lastFingerprintRef.current = newFingerprint;
      }
      setError(null);
      isInitialLoadRef.current = false;
    } catch (err) {
      if (err.name !== "AbortError") {
        console.error("Error fetching order flow data:", err);
        if (isInitialLoadRef.current || isManualRefetch) {
          setError(err.message || "Error fetching order flow data");
        }
      }
    } finally {
      if (isInitialLoadRef.current || isManualRefetch) {
        setIsLoading(false);
      }
    }
  };
  const refetch = () => {
    isManualRefetchRef.current = true;
    fetchData(true);
    isManualRefetchRef.current = false;
  };
  reactExports.useEffect(() => {
    if (!enabled) {
      if (intervalRef.current) {
        clearInterval(intervalRef.current);
        intervalRef.current = null;
      }
      return;
    }
    if (!data) {
      isInitialLoadRef.current = true;
    }
    fetchData(false);
    if (pollingInterval > 0) {
      intervalRef.current = setInterval(
        () => fetchData(false),
        pollingInterval
      );
    }
    return () => {
      if (abortControllerRef.current) {
        abortControllerRef.current.abort();
      }
      if (intervalRef.current) {
        clearInterval(intervalRef.current);
        intervalRef.current = null;
      }
    };
  }, [market, pollingInterval, enabled]);
  return {
    data,
    isLoading,
    error,
    lastUpdated,
    refetch
  };
}
function useOrderFlowStableMemory() {
  const memoryRef = reactExports.useRef({
    previousSpread: null,
    previousStats: null,
    previousPrice: 0,
    avgVolume: 0,
    avgSpread: 0
  });
  return {
    // Getters (return current ref values)
    get previousSpread() {
      return memoryRef.current.previousSpread;
    },
    get previousStats() {
      return memoryRef.current.previousStats;
    },
    get previousPrice() {
      return memoryRef.current.previousPrice;
    },
    get avgVolume() {
      return memoryRef.current.avgVolume;
    },
    get avgSpread() {
      return memoryRef.current.avgSpread;
    },
    // Setters (mutate ref without triggering re-render)
    setPreviousSpread: (spread) => {
      memoryRef.current.previousSpread = spread;
    },
    setPreviousStats: (stats) => {
      memoryRef.current.previousStats = stats;
    },
    setPreviousPrice: (price) => {
      memoryRef.current.previousPrice = price;
    },
    setAvgVolume: (volume) => {
      memoryRef.current.avgVolume = volume;
    },
    setAvgSpread: (spread) => {
      memoryRef.current.avgSpread = spread;
    },
    // Rolling average updaters
    updateAvgVolume: (currentVolume) => {
      if (memoryRef.current.avgVolume === 0) {
        memoryRef.current.avgVolume = currentVolume;
      } else {
        memoryRef.current.avgVolume = memoryRef.current.avgVolume * 0.95 + currentVolume * 0.05;
      }
    },
    updateAvgSpread: (currentSpread) => {
      if (memoryRef.current.avgSpread === 0) {
        memoryRef.current.avgSpread = currentSpread;
      } else {
        memoryRef.current.avgSpread = memoryRef.current.avgSpread * 0.95 + currentSpread * 0.05;
      }
    },
    // Reset all memory
    reset: () => {
      memoryRef.current = {
        previousSpread: null,
        previousStats: null,
        previousPrice: 0,
        avgVolume: 0,
        avgSpread: 0
      };
    }
  };
}
function calculateSpreadMetrics(bookTicker) {
  if (!bookTicker) return null;
  const bidPrice = Number.parseFloat(bookTicker.bidPrice);
  const askPrice = Number.parseFloat(bookTicker.askPrice);
  const spread = askPrice - bidPrice;
  const midPrice = (bidPrice + askPrice) / 2;
  const spreadPercent = spread / midPrice * 100;
  return {
    bidPrice,
    askPrice,
    spread,
    spreadPercent,
    midPrice
  };
}
function determineBookDirection(current, previous, threshold = 0.01) {
  if (!current || !previous) {
    return {
      bidTrend: "stable",
      askTrend: "stable",
      spreadTrend: "stable"
    };
  }
  const bidChange = (current.bidPrice - previous.bidPrice) / previous.bidPrice * 100;
  const askChange = (current.askPrice - previous.askPrice) / previous.askPrice * 100;
  const spreadChange = (current.spreadPercent - previous.spreadPercent) / previous.spreadPercent * 100;
  return {
    bidTrend: Math.abs(bidChange) < threshold ? "stable" : bidChange > 0 ? "up" : "down",
    askTrend: Math.abs(askChange) < threshold ? "stable" : askChange > 0 ? "up" : "down",
    spreadTrend: Math.abs(spreadChange) < threshold ? "stable" : spreadChange > 0 ? "widening" : "tightening"
  };
}
function detectConfluenceEvents(flowStats, currentSpread, previousSpread, direction, thresholds, previousEvents) {
  if (!currentSpread || !previousSpread) return previousEvents;
  const totalFlow = flowStats.totalBuyNotional + flowStats.totalSellNotional;
  if (totalFlow === 0) return previousEvents;
  const imbalancePercent = flowStats.netDelta / totalFlow * 100;
  const spreadChangePercent = (currentSpread.spreadPercent - previousSpread.spreadPercent) / previousSpread.spreadPercent * 100;
  const hasSignificantImbalance = Math.abs(imbalancePercent) >= thresholds.minImbalancePercent;
  const hasSignificantSpreadChange = Math.abs(spreadChangePercent) >= thresholds.minSpreadChangePercent;
  if (!hasSignificantImbalance) return previousEvents;
  const now = Date.now();
  const isBuyPressure = imbalancePercent > 0;
  if (isBuyPressure && (direction.spreadTrend === "tightening" || direction.bidTrend === "up") && hasSignificantSpreadChange) {
    const event = {
      id: `confluence_${now}`,
      timestamp: now,
      type: "buy_confluence",
      description: `Pressão de compra (${imbalancePercent.toFixed(1)}%) + ${direction.spreadTrend === "tightening" ? "spread fechando" : "bid subindo"}`,
      severity: Math.abs(imbalancePercent) > 70 ? "high" : Math.abs(imbalancePercent) > 50 ? "medium" : "low",
      metrics: {
        flowImbalance: imbalancePercent,
        spreadChange: spreadChangePercent,
        bidAskMovement: `Bid ${direction.bidTrend}, Ask ${direction.askTrend}`
      }
    };
    return [event, ...previousEvents].slice(0, 20);
  }
  if (!isBuyPressure && (direction.spreadTrend === "widening" || direction.askTrend === "down") && hasSignificantSpreadChange) {
    const event = {
      id: `confluence_${now}`,
      timestamp: now,
      type: "sell_confluence",
      description: `Pressão de venda (${Math.abs(imbalancePercent).toFixed(1)}%) + ${direction.spreadTrend === "widening" ? "spread abrindo" : "ask caindo"}`,
      severity: Math.abs(imbalancePercent) > 70 ? "high" : Math.abs(imbalancePercent) > 50 ? "medium" : "low",
      metrics: {
        flowImbalance: imbalancePercent,
        spreadChange: spreadChangePercent,
        bidAskMovement: `Bid ${direction.bidTrend}, Ask ${direction.askTrend}`
      }
    };
    return [event, ...previousEvents].slice(0, 20);
  }
  return previousEvents;
}
function generateProxyLiquidationAlert(currentStats, previousStats, currentPrice, previousPrice, thresholds) {
  if (!thresholds.enabled || !previousStats) return null;
  const volumeChange = currentStats.totalBuyNotional + currentStats.totalSellNotional;
  const previousVolume = previousStats.totalBuyNotional + previousStats.totalSellNotional;
  if (previousVolume === 0) return null;
  const volumeRatio = volumeChange / previousVolume;
  const priceChange = (currentPrice - previousPrice) / previousPrice * 100;
  const hasVolumeSpike = volumeRatio >= thresholds.volumeSpikeMultiplier;
  const hasPriceSpike = Math.abs(priceChange) >= thresholds.priceChangePercent;
  if (hasVolumeSpike && hasPriceSpike) {
    const severity = Math.abs(priceChange) > 5 ? "critical" : Math.abs(priceChange) > 3 ? "high" : Math.abs(priceChange) > 2 ? "medium" : "low";
    return {
      id: `liquidation_proxy_${Date.now()}`,
      timestamp: Date.now(),
      type: "liquidation_proxy",
      severity,
      title: "Possível Liquidação Detectada",
      description: `Spike de volume (${volumeRatio.toFixed(1)}x) + movimento de preço (${priceChange > 0 ? "+" : ""}${priceChange.toFixed(2)}%) pode indicar liquidações em cascata`,
      metrics: {
        volumeRatio,
        priceChange,
        totalVolume: volumeChange
      }
    };
  }
  return null;
}
function generateVolumeSpikeAlert(currentStats, avgVolume, thresholds) {
  if (!thresholds.enabled || avgVolume === 0) return null;
  const currentVolume = currentStats.totalBuyNotional + currentStats.totalSellNotional;
  const volumeRatio = currentVolume / avgVolume;
  if (volumeRatio >= thresholds.volumeSpikeMultiplier) {
    return {
      id: `volume_spike_${Date.now()}`,
      timestamp: Date.now(),
      type: "volume_spike",
      severity: volumeRatio > 5 ? "high" : volumeRatio > 3 ? "medium" : "low",
      title: "Spike de Volume",
      description: `Volume ${volumeRatio.toFixed(1)}x acima da média - possível entrada institucional`,
      metrics: {
        volumeRatio,
        currentVolume,
        avgVolume
      }
    };
  }
  return null;
}
function generateSpreadAnomalyAlert(currentSpread, avgSpread, thresholds) {
  if (!thresholds.enabled || !currentSpread || avgSpread === 0) return null;
  const spreadRatio = currentSpread.spreadPercent / avgSpread;
  if (spreadRatio >= thresholds.spreadAnomalyMultiplier) {
    return {
      id: `spread_anomaly_${Date.now()}`,
      timestamp: Date.now(),
      type: "spread_anomaly",
      severity: spreadRatio > 3 ? "high" : spreadRatio > 2 ? "medium" : "low",
      title: "Anomalia no Spread",
      description: `Spread ${spreadRatio.toFixed(1)}x maior que a média - possível baixa liquidez ou manipulação`,
      metrics: {
        spreadRatio,
        currentSpread: currentSpread.spreadPercent,
        avgSpread
      }
    };
  }
  return null;
}
function classifyTradeSide(trade) {
  return trade.isBuyerMaker ? "sell" : "buy";
}
function calculateNotional(trade) {
  return Number.parseFloat(trade.price) * Number.parseFloat(trade.qty);
}
function classifyTrades(trades, thresholds) {
  return trades.map((trade) => {
    const notional = calculateNotional(trade);
    return {
      trade,
      side: classifyTradeSide(trade),
      notional,
      isLarge: notional >= thresholds.largeTradeNotional
    };
  });
}
function calculateRollingStats(classifications, thresholds) {
  var _a, _b;
  if (classifications.length === 0) {
    return {
      totalBuyNotional: 0,
      totalSellNotional: 0,
      netDelta: 0,
      buyCount: 0,
      sellCount: 0,
      largeTradeCount: 0,
      avgTradeSize: 0,
      windowStart: Date.now(),
      windowEnd: Date.now()
    };
  }
  const windowByCount = classifications.slice(
    0,
    thresholds.rollingWindowTrades
  );
  const now = Date.now();
  const windowMs = thresholds.rollingWindowMinutes * 60 * 1e3;
  const windowByTime = classifications.filter(
    (c) => now - c.trade.time < windowMs
  );
  const window2 = windowByCount.length < windowByTime.length ? windowByCount : windowByTime;
  let totalBuyNotional = 0;
  let totalSellNotional = 0;
  let buyCount = 0;
  let sellCount = 0;
  let largeTradeCount = 0;
  for (const c of window2) {
    if (c.side === "buy") {
      totalBuyNotional += c.notional;
      buyCount++;
    } else {
      totalSellNotional += c.notional;
      sellCount++;
    }
    if (c.isLarge) largeTradeCount++;
  }
  const netDelta = totalBuyNotional - totalSellNotional;
  const totalNotional = totalBuyNotional + totalSellNotional;
  const avgTradeSize = window2.length > 0 ? totalNotional / window2.length : 0;
  return {
    totalBuyNotional,
    totalSellNotional,
    netDelta,
    buyCount,
    sellCount,
    largeTradeCount,
    avgTradeSize,
    windowStart: ((_a = window2[window2.length - 1]) == null ? void 0 : _a.trade.time) || now,
    windowEnd: ((_b = window2[0]) == null ? void 0 : _b.trade.time) || now
  };
}
function detectLargeTradeCluster(classifications, clusterWindow = 6e4, minClusterSize = 3) {
  const largeTrades = classifications.filter((c) => c.isLarge);
  if (largeTrades.length < minClusterSize) return false;
  const recentLarge = largeTrades.slice(0, minClusterSize);
  const timeSpan = recentLarge[0].trade.time - recentLarge[minClusterSize - 1].trade.time;
  return timeSpan <= clusterWindow;
}
function safeJsonParse(json, defaultValue) {
  if (!json) return defaultValue;
  try {
    const parsed = JSON.parse(json);
    return parsed !== null && typeof parsed === "object" ? parsed : defaultValue;
  } catch {
    return defaultValue;
  }
}
function validateOrderFlowThresholds(input) {
  const defaults = {
    largeTradeNotional: 1e5,
    rollingWindowTrades: 50,
    rollingWindowMinutes: 5
  };
  if (!input || typeof input !== "object") return defaults;
  return {
    largeTradeNotional: typeof input.largeTradeNotional === "number" && input.largeTradeNotional > 0 ? input.largeTradeNotional : defaults.largeTradeNotional,
    rollingWindowTrades: typeof input.rollingWindowTrades === "number" && input.rollingWindowTrades > 0 ? input.rollingWindowTrades : defaults.rollingWindowTrades,
    rollingWindowMinutes: typeof input.rollingWindowMinutes === "number" && input.rollingWindowMinutes > 0 ? input.rollingWindowMinutes : defaults.rollingWindowMinutes
  };
}
function validateConfluenceThresholds(input) {
  const defaults = {
    minImbalancePercent: 30,
    minSpreadChangePercent: 10,
    detectionWindowMs: 6e4
  };
  if (!input || typeof input !== "object") return defaults;
  return {
    minImbalancePercent: typeof input.minImbalancePercent === "number" && input.minImbalancePercent > 0 ? input.minImbalancePercent : defaults.minImbalancePercent,
    minSpreadChangePercent: typeof input.minSpreadChangePercent === "number" && input.minSpreadChangePercent > 0 ? input.minSpreadChangePercent : defaults.minSpreadChangePercent,
    detectionWindowMs: typeof input.detectionWindowMs === "number" && input.detectionWindowMs > 0 ? input.detectionWindowMs : defaults.detectionWindowMs
  };
}
function validateAlertThresholds(input) {
  const defaults = {
    volumeSpikeMultiplier: 2.5,
    priceChangePercent: 1.5,
    spreadAnomalyMultiplier: 2,
    enabled: true
  };
  if (!input || typeof input !== "object") return defaults;
  return {
    volumeSpikeMultiplier: typeof input.volumeSpikeMultiplier === "number" && input.volumeSpikeMultiplier > 0 ? input.volumeSpikeMultiplier : defaults.volumeSpikeMultiplier,
    priceChangePercent: typeof input.priceChangePercent === "number" && input.priceChangePercent > 0 ? input.priceChangePercent : defaults.priceChangePercent,
    spreadAnomalyMultiplier: typeof input.spreadAnomalyMultiplier === "number" && input.spreadAnomalyMultiplier > 0 ? input.spreadAnomalyMultiplier : defaults.spreadAnomalyMultiplier,
    enabled: typeof input.enabled === "boolean" ? input.enabled : defaults.enabled
  };
}
var useLayoutEffect2 = (globalThis == null ? void 0 : globalThis.document) ? reactExports.useLayoutEffect : () => {
};
function useStateMachine$1(initialState, machine) {
  return reactExports.useReducer((state, event) => {
    const nextState = machine[state][event];
    return nextState ?? state;
  }, initialState);
}
var Presence = (props) => {
  const { present, children } = props;
  const presence = usePresence(present);
  const child = typeof children === "function" ? children({ present: presence.isPresent }) : reactExports.Children.only(children);
  const ref = useComposedRefs(presence.ref, getElementRef(child));
  const forceMount = typeof children === "function";
  return forceMount || presence.isPresent ? reactExports.cloneElement(child, { ref }) : null;
};
Presence.displayName = "Presence";
function usePresence(present) {
  const [node, setNode] = reactExports.useState();
  const stylesRef = reactExports.useRef(null);
  const prevPresentRef = reactExports.useRef(present);
  const prevAnimationNameRef = reactExports.useRef("none");
  const initialState = present ? "mounted" : "unmounted";
  const [state, send] = useStateMachine$1(initialState, {
    mounted: {
      UNMOUNT: "unmounted",
      ANIMATION_OUT: "unmountSuspended"
    },
    unmountSuspended: {
      MOUNT: "mounted",
      ANIMATION_END: "unmounted"
    },
    unmounted: {
      MOUNT: "mounted"
    }
  });
  reactExports.useEffect(() => {
    const currentAnimationName = getAnimationName(stylesRef.current);
    prevAnimationNameRef.current = state === "mounted" ? currentAnimationName : "none";
  }, [state]);
  useLayoutEffect2(() => {
    const styles = stylesRef.current;
    const wasPresent = prevPresentRef.current;
    const hasPresentChanged = wasPresent !== present;
    if (hasPresentChanged) {
      const prevAnimationName = prevAnimationNameRef.current;
      const currentAnimationName = getAnimationName(styles);
      if (present) {
        send("MOUNT");
      } else if (currentAnimationName === "none" || (styles == null ? void 0 : styles.display) === "none") {
        send("UNMOUNT");
      } else {
        const isAnimating = prevAnimationName !== currentAnimationName;
        if (wasPresent && isAnimating) {
          send("ANIMATION_OUT");
        } else {
          send("UNMOUNT");
        }
      }
      prevPresentRef.current = present;
    }
  }, [present, send]);
  useLayoutEffect2(() => {
    if (node) {
      let timeoutId;
      const ownerWindow = node.ownerDocument.defaultView ?? window;
      const handleAnimationEnd = (event) => {
        const currentAnimationName = getAnimationName(stylesRef.current);
        const isCurrentAnimation = currentAnimationName.includes(CSS.escape(event.animationName));
        if (event.target === node && isCurrentAnimation) {
          send("ANIMATION_END");
          if (!prevPresentRef.current) {
            const currentFillMode = node.style.animationFillMode;
            node.style.animationFillMode = "forwards";
            timeoutId = ownerWindow.setTimeout(() => {
              if (node.style.animationFillMode === "forwards") {
                node.style.animationFillMode = currentFillMode;
              }
            });
          }
        }
      };
      const handleAnimationStart = (event) => {
        if (event.target === node) {
          prevAnimationNameRef.current = getAnimationName(stylesRef.current);
        }
      };
      node.addEventListener("animationstart", handleAnimationStart);
      node.addEventListener("animationcancel", handleAnimationEnd);
      node.addEventListener("animationend", handleAnimationEnd);
      return () => {
        ownerWindow.clearTimeout(timeoutId);
        node.removeEventListener("animationstart", handleAnimationStart);
        node.removeEventListener("animationcancel", handleAnimationEnd);
        node.removeEventListener("animationend", handleAnimationEnd);
      };
    } else {
      send("ANIMATION_END");
    }
  }, [node, send]);
  return {
    isPresent: ["mounted", "unmountSuspended"].includes(state),
    ref: reactExports.useCallback((node2) => {
      stylesRef.current = node2 ? getComputedStyle(node2) : null;
      setNode(node2);
    }, [])
  };
}
function getAnimationName(styles) {
  return (styles == null ? void 0 : styles.animationName) || "none";
}
function getElementRef(element) {
  var _a, _b;
  let getter = (_a = Object.getOwnPropertyDescriptor(element.props, "ref")) == null ? void 0 : _a.get;
  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element.ref;
  }
  getter = (_b = Object.getOwnPropertyDescriptor(element, "ref")) == null ? void 0 : _b.get;
  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
  if (mayWarn) {
    return element.props.ref;
  }
  return element.props.ref || element.ref;
}
function useCallbackRef(callback) {
  const callbackRef = reactExports.useRef(callback);
  reactExports.useEffect(() => {
    callbackRef.current = callback;
  });
  return reactExports.useMemo(() => (...args) => {
    var _a;
    return (_a = callbackRef.current) == null ? void 0 : _a.call(callbackRef, ...args);
  }, []);
}
var DirectionContext = reactExports.createContext(void 0);
function useDirection(localDir) {
  const globalDir = reactExports.useContext(DirectionContext);
  return localDir || globalDir || "ltr";
}
function clamp(value, [min, max]) {
  return Math.min(max, Math.max(min, value));
}
function composeEventHandlers(originalEventHandler, ourEventHandler, { checkForDefaultPrevented = true } = {}) {
  return function handleEvent(event) {
    originalEventHandler == null ? void 0 : originalEventHandler(event);
    if (checkForDefaultPrevented === false || !event.defaultPrevented) {
      return ourEventHandler == null ? void 0 : ourEventHandler(event);
    }
  };
}
function useStateMachine(initialState, machine) {
  return reactExports.useReducer((state, event) => {
    const nextState = machine[state][event];
    return nextState ?? state;
  }, initialState);
}
var SCROLL_AREA_NAME = "ScrollArea";
var [createScrollAreaContext] = createContextScope(SCROLL_AREA_NAME);
var [ScrollAreaProvider, useScrollAreaContext] = createScrollAreaContext(SCROLL_AREA_NAME);
var ScrollArea$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeScrollArea,
      type = "hover",
      dir,
      scrollHideDelay = 600,
      ...scrollAreaProps
    } = props;
    const [scrollArea, setScrollArea] = reactExports.useState(null);
    const [viewport, setViewport] = reactExports.useState(null);
    const [content, setContent] = reactExports.useState(null);
    const [scrollbarX, setScrollbarX] = reactExports.useState(null);
    const [scrollbarY, setScrollbarY] = reactExports.useState(null);
    const [cornerWidth, setCornerWidth] = reactExports.useState(0);
    const [cornerHeight, setCornerHeight] = reactExports.useState(0);
    const [scrollbarXEnabled, setScrollbarXEnabled] = reactExports.useState(false);
    const [scrollbarYEnabled, setScrollbarYEnabled] = reactExports.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setScrollArea(node));
    const direction = useDirection(dir);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaProvider,
      {
        scope: __scopeScrollArea,
        type,
        dir: direction,
        scrollHideDelay,
        scrollArea,
        viewport,
        onViewportChange: setViewport,
        content,
        onContentChange: setContent,
        scrollbarX,
        onScrollbarXChange: setScrollbarX,
        scrollbarXEnabled,
        onScrollbarXEnabledChange: setScrollbarXEnabled,
        scrollbarY,
        onScrollbarYChange: setScrollbarY,
        scrollbarYEnabled,
        onScrollbarYEnabledChange: setScrollbarYEnabled,
        onCornerWidthChange: setCornerWidth,
        onCornerHeightChange: setCornerHeight,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          Primitive.div,
          {
            dir: direction,
            ...scrollAreaProps,
            ref: composedRefs,
            style: {
              position: "relative",
              // Pass corner sizes as CSS vars to reduce re-renders of context consumers
              ["--radix-scroll-area-corner-width"]: cornerWidth + "px",
              ["--radix-scroll-area-corner-height"]: cornerHeight + "px",
              ...props.style
            }
          }
        )
      }
    );
  }
);
ScrollArea$1.displayName = SCROLL_AREA_NAME;
var VIEWPORT_NAME = "ScrollAreaViewport";
var ScrollAreaViewport = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, children, nonce, ...viewportProps } = props;
    const context = useScrollAreaContext(VIEWPORT_NAME, __scopeScrollArea);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref, context.onViewportChange);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "style",
        {
          dangerouslySetInnerHTML: {
            __html: `[data-radix-scroll-area-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-scroll-area-viewport]::-webkit-scrollbar{display:none}`
          },
          nonce
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          "data-radix-scroll-area-viewport": "",
          ...viewportProps,
          ref: composedRefs,
          style: {
            /**
             * We don't support `visible` because the intention is to have at least one scrollbar
             * if this component is used and `visible` will behave like `auto` in that case
             * https://developer.mozilla.org/en-US/docs/Web/CSS/overflow#description
             *
             * We don't handle `auto` because the intention is for the native implementation
             * to be hidden if using this component. We just want to ensure the node is scrollable
             * so could have used either `scroll` or `auto` here. We picked `scroll` to prevent
             * the browser from having to work out whether to render native scrollbars or not,
             * we tell it to with the intention of hiding them in CSS.
             */
            overflowX: context.scrollbarXEnabled ? "scroll" : "hidden",
            overflowY: context.scrollbarYEnabled ? "scroll" : "hidden",
            ...props.style
          },
          children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: context.onContentChange, style: { minWidth: "100%", display: "table" }, children })
        }
      )
    ] });
  }
);
ScrollAreaViewport.displayName = VIEWPORT_NAME;
var SCROLLBAR_NAME = "ScrollAreaScrollbar";
var ScrollAreaScrollbar = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...scrollbarProps } = props;
    const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
    const { onScrollbarXEnabledChange, onScrollbarYEnabledChange } = context;
    const isHorizontal = props.orientation === "horizontal";
    reactExports.useEffect(() => {
      isHorizontal ? onScrollbarXEnabledChange(true) : onScrollbarYEnabledChange(true);
      return () => {
        isHorizontal ? onScrollbarXEnabledChange(false) : onScrollbarYEnabledChange(false);
      };
    }, [isHorizontal, onScrollbarXEnabledChange, onScrollbarYEnabledChange]);
    return context.type === "hover" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarHover, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "scroll" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarScroll, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "auto" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarAuto, { ...scrollbarProps, ref: forwardedRef, forceMount }) : context.type === "always" ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaScrollbarVisible, { ...scrollbarProps, ref: forwardedRef }) : null;
  }
);
ScrollAreaScrollbar.displayName = SCROLLBAR_NAME;
var ScrollAreaScrollbarHover = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [visible, setVisible] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const scrollArea = context.scrollArea;
    let hideTimer = 0;
    if (scrollArea) {
      const handlePointerEnter = () => {
        window.clearTimeout(hideTimer);
        setVisible(true);
      };
      const handlePointerLeave = () => {
        hideTimer = window.setTimeout(() => setVisible(false), context.scrollHideDelay);
      };
      scrollArea.addEventListener("pointerenter", handlePointerEnter);
      scrollArea.addEventListener("pointerleave", handlePointerLeave);
      return () => {
        window.clearTimeout(hideTimer);
        scrollArea.removeEventListener("pointerenter", handlePointerEnter);
        scrollArea.removeEventListener("pointerleave", handlePointerLeave);
      };
    }
  }, [context.scrollArea, context.scrollHideDelay]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarAuto,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarScroll = reactExports.forwardRef((props, forwardedRef) => {
  const { forceMount, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const isHorizontal = props.orientation === "horizontal";
  const debounceScrollEnd = useDebounceCallback(() => send("SCROLL_END"), 100);
  const [state, send] = useStateMachine("hidden", {
    hidden: {
      SCROLL: "scrolling"
    },
    scrolling: {
      SCROLL_END: "idle",
      POINTER_ENTER: "interacting"
    },
    interacting: {
      SCROLL: "interacting",
      POINTER_LEAVE: "idle"
    },
    idle: {
      HIDE: "hidden",
      SCROLL: "scrolling",
      POINTER_ENTER: "interacting"
    }
  });
  reactExports.useEffect(() => {
    if (state === "idle") {
      const hideTimer = window.setTimeout(() => send("HIDE"), context.scrollHideDelay);
      return () => window.clearTimeout(hideTimer);
    }
  }, [state, context.scrollHideDelay, send]);
  reactExports.useEffect(() => {
    const viewport = context.viewport;
    const scrollDirection = isHorizontal ? "scrollLeft" : "scrollTop";
    if (viewport) {
      let prevScrollPos = viewport[scrollDirection];
      const handleScroll = () => {
        const scrollPos = viewport[scrollDirection];
        const hasScrollInDirectionChanged = prevScrollPos !== scrollPos;
        if (hasScrollInDirectionChanged) {
          send("SCROLL");
          debounceScrollEnd();
        }
        prevScrollPos = scrollPos;
      };
      viewport.addEventListener("scroll", handleScroll);
      return () => viewport.removeEventListener("scroll", handleScroll);
    }
  }, [context.viewport, isHorizontal, send, debounceScrollEnd]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || state !== "hidden", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": state === "hidden" ? "hidden" : "visible",
      ...scrollbarProps,
      ref: forwardedRef,
      onPointerEnter: composeEventHandlers(props.onPointerEnter, () => send("POINTER_ENTER")),
      onPointerLeave: composeEventHandlers(props.onPointerLeave, () => send("POINTER_LEAVE"))
    }
  ) });
});
var ScrollAreaScrollbarAuto = reactExports.forwardRef((props, forwardedRef) => {
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const { forceMount, ...scrollbarProps } = props;
  const [visible, setVisible] = reactExports.useState(false);
  const isHorizontal = props.orientation === "horizontal";
  const handleResize = useDebounceCallback(() => {
    if (context.viewport) {
      const isOverflowX = context.viewport.offsetWidth < context.viewport.scrollWidth;
      const isOverflowY = context.viewport.offsetHeight < context.viewport.scrollHeight;
      setVisible(isHorizontal ? isOverflowX : isOverflowY);
    }
  }, 10);
  useResizeObserver(context.viewport, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || visible, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarVisible,
    {
      "data-state": visible ? "visible" : "hidden",
      ...scrollbarProps,
      ref: forwardedRef
    }
  ) });
});
var ScrollAreaScrollbarVisible = reactExports.forwardRef((props, forwardedRef) => {
  const { orientation = "vertical", ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const thumbRef = reactExports.useRef(null);
  const pointerOffsetRef = reactExports.useRef(0);
  const [sizes, setSizes] = reactExports.useState({
    content: 0,
    viewport: 0,
    scrollbar: { size: 0, paddingStart: 0, paddingEnd: 0 }
  });
  const thumbRatio = getThumbRatio(sizes.viewport, sizes.content);
  const commonProps = {
    ...scrollbarProps,
    sizes,
    onSizesChange: setSizes,
    hasThumb: Boolean(thumbRatio > 0 && thumbRatio < 1),
    onThumbChange: (thumb) => thumbRef.current = thumb,
    onThumbPointerUp: () => pointerOffsetRef.current = 0,
    onThumbPointerDown: (pointerPos) => pointerOffsetRef.current = pointerPos
  };
  function getScrollPosition(pointerPos, dir) {
    return getScrollPositionFromPointer(pointerPos, pointerOffsetRef.current, sizes, dir);
  }
  if (orientation === "horizontal") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarX,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollLeft;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes, context.dir);
            thumbRef.current.style.transform = `translate3d(${offset}px, 0, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollLeft = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) {
            context.viewport.scrollLeft = getScrollPosition(pointerPos, context.dir);
          }
        }
      }
    );
  }
  if (orientation === "vertical") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      ScrollAreaScrollbarY,
      {
        ...commonProps,
        ref: forwardedRef,
        onThumbPositionChange: () => {
          if (context.viewport && thumbRef.current) {
            const scrollPos = context.viewport.scrollTop;
            const offset = getThumbOffsetFromScroll(scrollPos, sizes);
            thumbRef.current.style.transform = `translate3d(0, ${offset}px, 0)`;
          }
        },
        onWheelScroll: (scrollPos) => {
          if (context.viewport) context.viewport.scrollTop = scrollPos;
        },
        onDragScroll: (pointerPos) => {
          if (context.viewport) context.viewport.scrollTop = getScrollPosition(pointerPos);
        }
      }
    );
  }
  return null;
});
var ScrollAreaScrollbarX = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarXChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "horizontal",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        bottom: 0,
        left: context.dir === "rtl" ? "var(--radix-scroll-area-corner-width)" : 0,
        right: context.dir === "ltr" ? "var(--radix-scroll-area-corner-width)" : 0,
        ["--radix-scroll-area-thumb-width"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.x),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.x),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollLeft + event.deltaX;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollWidth,
            viewport: context.viewport.offsetWidth,
            scrollbar: {
              size: ref.current.clientWidth,
              paddingStart: toInt(computedStyle.paddingLeft),
              paddingEnd: toInt(computedStyle.paddingRight)
            }
          });
        }
      }
    }
  );
});
var ScrollAreaScrollbarY = reactExports.forwardRef((props, forwardedRef) => {
  const { sizes, onSizesChange, ...scrollbarProps } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, props.__scopeScrollArea);
  const [computedStyle, setComputedStyle] = reactExports.useState();
  const ref = reactExports.useRef(null);
  const composeRefs = useComposedRefs(forwardedRef, ref, context.onScrollbarYChange);
  reactExports.useEffect(() => {
    if (ref.current) setComputedStyle(getComputedStyle(ref.current));
  }, [ref]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbarImpl,
    {
      "data-orientation": "vertical",
      ...scrollbarProps,
      ref: composeRefs,
      sizes,
      style: {
        top: 0,
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: "var(--radix-scroll-area-corner-height)",
        ["--radix-scroll-area-thumb-height"]: getThumbSize(sizes) + "px",
        ...props.style
      },
      onThumbPointerDown: (pointerPos) => props.onThumbPointerDown(pointerPos.y),
      onDragScroll: (pointerPos) => props.onDragScroll(pointerPos.y),
      onWheelScroll: (event, maxScrollPos) => {
        if (context.viewport) {
          const scrollPos = context.viewport.scrollTop + event.deltaY;
          props.onWheelScroll(scrollPos);
          if (isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos)) {
            event.preventDefault();
          }
        }
      },
      onResize: () => {
        if (ref.current && context.viewport && computedStyle) {
          onSizesChange({
            content: context.viewport.scrollHeight,
            viewport: context.viewport.offsetHeight,
            scrollbar: {
              size: ref.current.clientHeight,
              paddingStart: toInt(computedStyle.paddingTop),
              paddingEnd: toInt(computedStyle.paddingBottom)
            }
          });
        }
      }
    }
  );
});
var [ScrollbarProvider, useScrollbarContext] = createScrollAreaContext(SCROLLBAR_NAME);
var ScrollAreaScrollbarImpl = reactExports.forwardRef((props, forwardedRef) => {
  const {
    __scopeScrollArea,
    sizes,
    hasThumb,
    onThumbChange,
    onThumbPointerUp,
    onThumbPointerDown,
    onThumbPositionChange,
    onDragScroll,
    onWheelScroll,
    onResize,
    ...scrollbarProps
  } = props;
  const context = useScrollAreaContext(SCROLLBAR_NAME, __scopeScrollArea);
  const [scrollbar, setScrollbar] = reactExports.useState(null);
  const composeRefs = useComposedRefs(forwardedRef, (node) => setScrollbar(node));
  const rectRef = reactExports.useRef(null);
  const prevWebkitUserSelectRef = reactExports.useRef("");
  const viewport = context.viewport;
  const maxScrollPos = sizes.content - sizes.viewport;
  const handleWheelScroll = useCallbackRef(onWheelScroll);
  const handleThumbPositionChange = useCallbackRef(onThumbPositionChange);
  const handleResize = useDebounceCallback(onResize, 10);
  function handleDragScroll(event) {
    if (rectRef.current) {
      const x = event.clientX - rectRef.current.left;
      const y = event.clientY - rectRef.current.top;
      onDragScroll({ x, y });
    }
  }
  reactExports.useEffect(() => {
    const handleWheel = (event) => {
      const element = event.target;
      const isScrollbarWheel = scrollbar == null ? void 0 : scrollbar.contains(element);
      if (isScrollbarWheel) handleWheelScroll(event, maxScrollPos);
    };
    document.addEventListener("wheel", handleWheel, { passive: false });
    return () => document.removeEventListener("wheel", handleWheel, { passive: false });
  }, [viewport, scrollbar, maxScrollPos, handleWheelScroll]);
  reactExports.useEffect(handleThumbPositionChange, [sizes, handleThumbPositionChange]);
  useResizeObserver(scrollbar, handleResize);
  useResizeObserver(context.content, handleResize);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollbarProvider,
    {
      scope: __scopeScrollArea,
      scrollbar,
      hasThumb,
      onThumbChange: useCallbackRef(onThumbChange),
      onThumbPointerUp: useCallbackRef(onThumbPointerUp),
      onThumbPositionChange: handleThumbPositionChange,
      onThumbPointerDown: useCallbackRef(onThumbPointerDown),
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.div,
        {
          ...scrollbarProps,
          ref: composeRefs,
          style: { position: "absolute", ...scrollbarProps.style },
          onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
            const mainPointer = 0;
            if (event.button === mainPointer) {
              const element = event.target;
              element.setPointerCapture(event.pointerId);
              rectRef.current = scrollbar.getBoundingClientRect();
              prevWebkitUserSelectRef.current = document.body.style.webkitUserSelect;
              document.body.style.webkitUserSelect = "none";
              if (context.viewport) context.viewport.style.scrollBehavior = "auto";
              handleDragScroll(event);
            }
          }),
          onPointerMove: composeEventHandlers(props.onPointerMove, handleDragScroll),
          onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
            const element = event.target;
            if (element.hasPointerCapture(event.pointerId)) {
              element.releasePointerCapture(event.pointerId);
            }
            document.body.style.webkitUserSelect = prevWebkitUserSelectRef.current;
            if (context.viewport) context.viewport.style.scrollBehavior = "";
            rectRef.current = null;
          })
        }
      )
    }
  );
});
var THUMB_NAME$1 = "ScrollAreaThumb";
var ScrollAreaThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { forceMount, ...thumbProps } = props;
    const scrollbarContext = useScrollbarContext(THUMB_NAME$1, props.__scopeScrollArea);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Presence, { present: forceMount || scrollbarContext.hasThumb, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaThumbImpl, { ref: forwardedRef, ...thumbProps }) });
  }
);
var ScrollAreaThumbImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeScrollArea, style, ...thumbProps } = props;
    const scrollAreaContext = useScrollAreaContext(THUMB_NAME$1, __scopeScrollArea);
    const scrollbarContext = useScrollbarContext(THUMB_NAME$1, __scopeScrollArea);
    const { onThumbPositionChange } = scrollbarContext;
    const composedRef = useComposedRefs(
      forwardedRef,
      (node) => scrollbarContext.onThumbChange(node)
    );
    const removeUnlinkedScrollListenerRef = reactExports.useRef(void 0);
    const debounceScrollEnd = useDebounceCallback(() => {
      if (removeUnlinkedScrollListenerRef.current) {
        removeUnlinkedScrollListenerRef.current();
        removeUnlinkedScrollListenerRef.current = void 0;
      }
    }, 100);
    reactExports.useEffect(() => {
      const viewport = scrollAreaContext.viewport;
      if (viewport) {
        const handleScroll = () => {
          debounceScrollEnd();
          if (!removeUnlinkedScrollListenerRef.current) {
            const listener = addUnlinkedScrollListener(viewport, onThumbPositionChange);
            removeUnlinkedScrollListenerRef.current = listener;
            onThumbPositionChange();
          }
        };
        onThumbPositionChange();
        viewport.addEventListener("scroll", handleScroll);
        return () => viewport.removeEventListener("scroll", handleScroll);
      }
    }, [scrollAreaContext.viewport, debounceScrollEnd, onThumbPositionChange]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.div,
      {
        "data-state": scrollbarContext.hasThumb ? "visible" : "hidden",
        ...thumbProps,
        ref: composedRef,
        style: {
          width: "var(--radix-scroll-area-thumb-width)",
          height: "var(--radix-scroll-area-thumb-height)",
          ...style
        },
        onPointerDownCapture: composeEventHandlers(props.onPointerDownCapture, (event) => {
          const thumb = event.target;
          const thumbRect = thumb.getBoundingClientRect();
          const x = event.clientX - thumbRect.left;
          const y = event.clientY - thumbRect.top;
          scrollbarContext.onThumbPointerDown({ x, y });
        }),
        onPointerUp: composeEventHandlers(props.onPointerUp, scrollbarContext.onThumbPointerUp)
      }
    );
  }
);
ScrollAreaThumb.displayName = THUMB_NAME$1;
var CORNER_NAME = "ScrollAreaCorner";
var ScrollAreaCorner = reactExports.forwardRef(
  (props, forwardedRef) => {
    const context = useScrollAreaContext(CORNER_NAME, props.__scopeScrollArea);
    const hasBothScrollbarsVisible = Boolean(context.scrollbarX && context.scrollbarY);
    const hasCorner = context.type !== "scroll" && hasBothScrollbarsVisible;
    return hasCorner ? /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollAreaCornerImpl, { ...props, ref: forwardedRef }) : null;
  }
);
ScrollAreaCorner.displayName = CORNER_NAME;
var ScrollAreaCornerImpl = reactExports.forwardRef((props, forwardedRef) => {
  const { __scopeScrollArea, ...cornerProps } = props;
  const context = useScrollAreaContext(CORNER_NAME, __scopeScrollArea);
  const [width, setWidth] = reactExports.useState(0);
  const [height, setHeight] = reactExports.useState(0);
  const hasSize = Boolean(width && height);
  useResizeObserver(context.scrollbarX, () => {
    var _a;
    const height2 = ((_a = context.scrollbarX) == null ? void 0 : _a.offsetHeight) || 0;
    context.onCornerHeightChange(height2);
    setHeight(height2);
  });
  useResizeObserver(context.scrollbarY, () => {
    var _a;
    const width2 = ((_a = context.scrollbarY) == null ? void 0 : _a.offsetWidth) || 0;
    context.onCornerWidthChange(width2);
    setWidth(width2);
  });
  return hasSize ? /* @__PURE__ */ jsxRuntimeExports.jsx(
    Primitive.div,
    {
      ...cornerProps,
      ref: forwardedRef,
      style: {
        width,
        height,
        position: "absolute",
        right: context.dir === "ltr" ? 0 : void 0,
        left: context.dir === "rtl" ? 0 : void 0,
        bottom: 0,
        ...props.style
      }
    }
  ) : null;
});
function toInt(value) {
  return value ? parseInt(value, 10) : 0;
}
function getThumbRatio(viewportSize, contentSize) {
  const ratio = viewportSize / contentSize;
  return isNaN(ratio) ? 0 : ratio;
}
function getThumbSize(sizes) {
  const ratio = getThumbRatio(sizes.viewport, sizes.content);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const thumbSize = (sizes.scrollbar.size - scrollbarPadding) * ratio;
  return Math.max(thumbSize, 18);
}
function getScrollPositionFromPointer(pointerPos, pointerOffset, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const thumbCenter = thumbSizePx / 2;
  const offset = pointerOffset || thumbCenter;
  const thumbOffsetFromEnd = thumbSizePx - offset;
  const minPointerPos = sizes.scrollbar.paddingStart + offset;
  const maxPointerPos = sizes.scrollbar.size - sizes.scrollbar.paddingEnd - thumbOffsetFromEnd;
  const maxScrollPos = sizes.content - sizes.viewport;
  const scrollRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const interpolate = linearScale([minPointerPos, maxPointerPos], scrollRange);
  return interpolate(pointerPos);
}
function getThumbOffsetFromScroll(scrollPos, sizes, dir = "ltr") {
  const thumbSizePx = getThumbSize(sizes);
  const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
  const scrollbar = sizes.scrollbar.size - scrollbarPadding;
  const maxScrollPos = sizes.content - sizes.viewport;
  const maxThumbPos = scrollbar - thumbSizePx;
  const scrollClampRange = dir === "ltr" ? [0, maxScrollPos] : [maxScrollPos * -1, 0];
  const scrollWithoutMomentum = clamp(scrollPos, scrollClampRange);
  const interpolate = linearScale([0, maxScrollPos], [0, maxThumbPos]);
  return interpolate(scrollWithoutMomentum);
}
function linearScale(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1]) return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function isScrollingWithinScrollbarBounds(scrollPos, maxScrollPos) {
  return scrollPos > 0 && scrollPos < maxScrollPos;
}
var addUnlinkedScrollListener = (node, handler = () => {
}) => {
  let prevPosition = { left: node.scrollLeft, top: node.scrollTop };
  let rAF = 0;
  (function loop() {
    const position = { left: node.scrollLeft, top: node.scrollTop };
    const isHorizontalScroll = prevPosition.left !== position.left;
    const isVerticalScroll = prevPosition.top !== position.top;
    if (isHorizontalScroll || isVerticalScroll) handler();
    prevPosition = position;
    rAF = window.requestAnimationFrame(loop);
  })();
  return () => window.cancelAnimationFrame(rAF);
};
function useDebounceCallback(callback, delay) {
  const handleCallback = useCallbackRef(callback);
  const debounceTimerRef = reactExports.useRef(0);
  reactExports.useEffect(() => () => window.clearTimeout(debounceTimerRef.current), []);
  return reactExports.useCallback(() => {
    window.clearTimeout(debounceTimerRef.current);
    debounceTimerRef.current = window.setTimeout(handleCallback, delay);
  }, [handleCallback, delay]);
}
function useResizeObserver(element, onResize) {
  const handleResize = useCallbackRef(onResize);
  useLayoutEffect2(() => {
    let rAF = 0;
    if (element) {
      const resizeObserver = new ResizeObserver(() => {
        cancelAnimationFrame(rAF);
        rAF = window.requestAnimationFrame(handleResize);
      });
      resizeObserver.observe(element);
      return () => {
        window.cancelAnimationFrame(rAF);
        resizeObserver.unobserve(element);
      };
    }
  }, [element, handleResize]);
}
var Root$1 = ScrollArea$1;
var Viewport = ScrollAreaViewport;
var Corner = ScrollAreaCorner;
function ScrollArea({
  className,
  children,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    Root$1,
    {
      "data-slot": "scroll-area",
      className: cn("relative", className),
      ...props,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Viewport,
          {
            "data-slot": "scroll-area-viewport",
            className: "focus-visible:ring-ring/50 size-full rounded-[inherit] transition-[color,box-shadow] outline-none focus-visible:ring-[3px] focus-visible:outline-1",
            children
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollBar, {}),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Corner, {})
      ]
    }
  );
}
function ScrollBar({
  className,
  orientation = "vertical",
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    ScrollAreaScrollbar,
    {
      "data-slot": "scroll-area-scrollbar",
      orientation,
      className: cn(
        "flex touch-none p-px transition-colors select-none",
        orientation === "vertical" && "h-full w-2.5 border-l border-l-transparent",
        orientation === "horizontal" && "h-2.5 flex-col border-t border-t-transparent",
        className
      ),
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        ScrollAreaThumb,
        {
          "data-slot": "scroll-area-thumb",
          className: "bg-border relative flex-1 rounded-full"
        }
      )
    }
  );
}
function OrderFlowSection({
  title,
  icon: Icon,
  badge,
  children,
  className = ""
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Card, { className: `terminal-panel ${className}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(CardHeader, { className: "pb-3", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(CardTitle, { className: "text-base font-semibold flex items-center gap-2", children: [
        Icon && /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "w-4 h-4" }),
        title
      ] }),
      badge
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(CardContent, { children })
  ] });
}
function AlertsPanel({
  alerts,
  onClearAlerts
}) {
  const formatTime = (timestamp) => {
    return new Date(timestamp).toLocaleTimeString("en-US", {
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit"
    });
  };
  const getSeverityColor = (severity) => {
    switch (severity) {
      case "critical":
      case "high":
        return "text-neon-pink border-neon-pink/50 bg-neon-pink/10";
      case "medium":
        return "text-neon-yellow border-neon-yellow/50 bg-neon-yellow/10";
      default:
        return "text-neon-cyan border-neon-cyan/50 bg-neon-cyan/10";
    }
  };
  const badge = /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
    alerts.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: "outline", className: "text-neon-pink border-neon-pink/50", children: COPY.alertsCount(alerts.length) }),
    alerts.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
      Button,
      {
        variant: "ghost",
        size: "sm",
        onClick: onClearAlerts,
        className: "h-7 text-xs text-muted-foreground hover:text-foreground",
        children: COPY.clearAlerts
      }
    )
  ] });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(OrderFlowSection, { title: COPY.alerts, icon: Bell, badge, children: alerts.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center justify-center py-8 text-center", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(BellOff, { className: "w-12 h-12 text-muted-foreground/30 mb-3" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground", children: COPY.noAlerts })
  ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "h-[300px] pr-4", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-3", children: alerts.map((alert, index) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: `p-3 rounded-lg border ${getSeverityColor(alert.severity)}`,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start justify-between gap-2 mb-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "w-4 h-4 flex-shrink-0" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold text-sm", children: alert.title })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: "outline", className: "text-xs", children: alert.severity })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mb-2", children: alert.description }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground", children: formatTime(alert.timestamp) })
      ]
    },
    `${alert.timestamp}-${index}`
  )) }) }) });
}
function ConfluencePanel({ events }) {
  const formatTime = (timestamp) => {
    return new Date(timestamp).toLocaleTimeString("en-US", {
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit"
    });
  };
  const getDirectionIcon = (description) => {
    if (description.includes("compra") || description.includes("buy")) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(TrendingUp, { className: "w-4 h-4 text-neon-green" });
    }
    if (description.includes("venda") || description.includes("sell")) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(TrendingDown, { className: "w-4 h-4 text-neon-pink" });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(Target, { className: "w-4 h-4 text-neon-cyan" });
  };
  const badge = events.length > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: "outline", className: "text-neon-cyan border-neon-cyan/50", children: events.length }) : null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(OrderFlowSection, { title: COPY.confluenceEvents, icon: Target, badge, children: events.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center justify-center py-8 text-center", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Target, { className: "w-12 h-12 text-muted-foreground/30 mb-3" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground", children: COPY.noConfluence })
  ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(ScrollArea, { className: "h-[300px] pr-4", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-3", children: events.map((event, index) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: "p-3 rounded-lg border border-neon-cyan/30 bg-neon-cyan/5",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start justify-between gap-2 mb-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
            getDirectionIcon(event.description),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold text-sm text-neon-cyan", children: event.type })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Badge,
            {
              variant: "outline",
              className: "text-xs text-neon-cyan border-neon-cyan/50",
              children: event.severity
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mb-2", children: event.description }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground", children: formatTime(event.timestamp) })
      ]
    },
    `${event.type}-${index}`
  )) }) }) });
}
var useInsertionEffect = React[" useInsertionEffect ".trim().toString()] || useLayoutEffect2;
function useControllableState({
  prop,
  defaultProp,
  onChange = () => {
  },
  caller
}) {
  const [uncontrolledProp, setUncontrolledProp, onChangeRef] = useUncontrolledState({
    defaultProp,
    onChange
  });
  const isControlled = prop !== void 0;
  const value = isControlled ? prop : uncontrolledProp;
  {
    const isControlledRef = reactExports.useRef(prop !== void 0);
    reactExports.useEffect(() => {
      const wasControlled = isControlledRef.current;
      if (wasControlled !== isControlled) {
        const from = wasControlled ? "controlled" : "uncontrolled";
        const to = isControlled ? "controlled" : "uncontrolled";
        console.warn(
          `${caller} is changing from ${from} to ${to}. Components should not switch from controlled to uncontrolled (or vice versa). Decide between using a controlled or uncontrolled value for the lifetime of the component.`
        );
      }
      isControlledRef.current = isControlled;
    }, [isControlled, caller]);
  }
  const setValue = reactExports.useCallback(
    (nextValue) => {
      var _a;
      if (isControlled) {
        const value2 = isFunction(nextValue) ? nextValue(prop) : nextValue;
        if (value2 !== prop) {
          (_a = onChangeRef.current) == null ? void 0 : _a.call(onChangeRef, value2);
        }
      } else {
        setUncontrolledProp(nextValue);
      }
    },
    [isControlled, prop, setUncontrolledProp, onChangeRef]
  );
  return [value, setValue];
}
function useUncontrolledState({
  defaultProp,
  onChange
}) {
  const [value, setValue] = reactExports.useState(defaultProp);
  const prevValueRef = reactExports.useRef(value);
  const onChangeRef = reactExports.useRef(onChange);
  useInsertionEffect(() => {
    onChangeRef.current = onChange;
  }, [onChange]);
  reactExports.useEffect(() => {
    var _a;
    if (prevValueRef.current !== value) {
      (_a = onChangeRef.current) == null ? void 0 : _a.call(onChangeRef, value);
      prevValueRef.current = value;
    }
  }, [value, prevValueRef]);
  return [value, setValue, onChangeRef];
}
function isFunction(value) {
  return typeof value === "function";
}
function usePrevious(value) {
  const ref = reactExports.useRef({ value, previous: value });
  return reactExports.useMemo(() => {
    if (ref.current.value !== value) {
      ref.current.previous = ref.current.value;
      ref.current.value = value;
    }
    return ref.current.previous;
  }, [value]);
}
function useSize(element) {
  const [size, setSize] = reactExports.useState(void 0);
  useLayoutEffect2(() => {
    if (element) {
      setSize({ width: element.offsetWidth, height: element.offsetHeight });
      const resizeObserver = new ResizeObserver((entries) => {
        if (!Array.isArray(entries)) {
          return;
        }
        if (!entries.length) {
          return;
        }
        const entry = entries[0];
        let width;
        let height;
        if ("borderBoxSize" in entry) {
          const borderSizeEntry = entry["borderBoxSize"];
          const borderSize = Array.isArray(borderSizeEntry) ? borderSizeEntry[0] : borderSizeEntry;
          width = borderSize["inlineSize"];
          height = borderSize["blockSize"];
        } else {
          width = element.offsetWidth;
          height = element.offsetHeight;
        }
        setSize({ width, height });
      });
      resizeObserver.observe(element, { box: "border-box" });
      return () => resizeObserver.unobserve(element);
    } else {
      setSize(void 0);
    }
  }, [element]);
  return size;
}
var SWITCH_NAME = "Switch";
var [createSwitchContext] = createContextScope(SWITCH_NAME);
var [SwitchProvider, useSwitchContext] = createSwitchContext(SWITCH_NAME);
var Switch$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeSwitch,
      name,
      checked: checkedProp,
      defaultChecked,
      required,
      disabled,
      value = "on",
      onCheckedChange,
      form,
      ...switchProps
    } = props;
    const [button, setButton] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node) => setButton(node));
    const hasConsumerStoppedPropagationRef = reactExports.useRef(false);
    const isFormControl = button ? form || !!button.closest("form") : true;
    const [checked, setChecked] = useControllableState({
      prop: checkedProp,
      defaultProp: defaultChecked ?? false,
      onChange: onCheckedChange,
      caller: SWITCH_NAME
    });
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(SwitchProvider, { scope: __scopeSwitch, checked, disabled, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Primitive.button,
        {
          type: "button",
          role: "switch",
          "aria-checked": checked,
          "aria-required": required,
          "data-state": getState(checked),
          "data-disabled": disabled ? "" : void 0,
          disabled,
          value,
          ...switchProps,
          ref: composedRefs,
          onClick: composeEventHandlers(props.onClick, (event) => {
            setChecked((prevChecked) => !prevChecked);
            if (isFormControl) {
              hasConsumerStoppedPropagationRef.current = event.isPropagationStopped();
              if (!hasConsumerStoppedPropagationRef.current) event.stopPropagation();
            }
          })
        }
      ),
      isFormControl && /* @__PURE__ */ jsxRuntimeExports.jsx(
        SwitchBubbleInput,
        {
          control: button,
          bubbles: !hasConsumerStoppedPropagationRef.current,
          name,
          value,
          checked,
          required,
          disabled,
          form,
          style: { transform: "translateX(-100%)" }
        }
      )
    ] });
  }
);
Switch$1.displayName = SWITCH_NAME;
var THUMB_NAME = "SwitchThumb";
var SwitchThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSwitch, ...thumbProps } = props;
    const context = useSwitchContext(THUMB_NAME, __scopeSwitch);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.span,
      {
        "data-state": getState(context.checked),
        "data-disabled": context.disabled ? "" : void 0,
        ...thumbProps,
        ref: forwardedRef
      }
    );
  }
);
SwitchThumb.displayName = THUMB_NAME;
var BUBBLE_INPUT_NAME = "SwitchBubbleInput";
var SwitchBubbleInput = reactExports.forwardRef(
  ({
    __scopeSwitch,
    control,
    checked,
    bubbles = true,
    ...props
  }, forwardedRef) => {
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(ref, forwardedRef);
    const prevChecked = usePrevious(checked);
    const controlSize = useSize(control);
    reactExports.useEffect(() => {
      const input = ref.current;
      if (!input) return;
      const inputProto = window.HTMLInputElement.prototype;
      const descriptor = Object.getOwnPropertyDescriptor(
        inputProto,
        "checked"
      );
      const setChecked = descriptor.set;
      if (prevChecked !== checked && setChecked) {
        const event = new Event("click", { bubbles });
        setChecked.call(input, checked);
        input.dispatchEvent(event);
      }
    }, [prevChecked, checked, bubbles]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      "input",
      {
        type: "checkbox",
        "aria-hidden": true,
        defaultChecked: checked,
        ...props,
        tabIndex: -1,
        ref: composedRefs,
        style: {
          ...props.style,
          ...controlSize,
          position: "absolute",
          pointerEvents: "none",
          opacity: 0,
          margin: 0
        }
      }
    );
  }
);
SwitchBubbleInput.displayName = BUBBLE_INPUT_NAME;
function getState(checked) {
  return checked ? "checked" : "unchecked";
}
var Root = Switch$1;
var Thumb = SwitchThumb;
function Switch({
  className,
  ...props
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    Root,
    {
      "data-slot": "switch",
      className: cn(
        "peer data-[state=checked]:bg-primary data-[state=unchecked]:bg-input focus-visible:border-ring focus-visible:ring-ring/50 dark:data-[state=unchecked]:bg-input/80 inline-flex h-[1.15rem] w-8 shrink-0 items-center rounded-full border border-transparent shadow-xs transition-all outline-none focus-visible:ring-[3px] disabled:cursor-not-allowed disabled:opacity-50",
        className
      ),
      ...props,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        Thumb,
        {
          "data-slot": "switch-thumb",
          className: cn(
            "bg-background dark:data-[state=unchecked]:bg-foreground dark:data-[state=checked]:bg-primary-foreground pointer-events-none block size-4 rounded-full ring-0 transition-transform data-[state=checked]:translate-x-[calc(100%-2px)] data-[state=unchecked]:translate-x-0"
          )
        }
      )
    }
  );
}
function OrderFlowControlsBar({
  market,
  onMarketChange,
  enabled,
  onEnabledChange,
  lastUpdated,
  isLoading,
  onRefresh,
  showSettings: _showSettings,
  onToggleSettings
}) {
  const formatTime = (timestamp) => {
    return new Date(timestamp).toLocaleTimeString("en-US", {
      hour: "2-digit",
      minute: "2-digit",
      second: "2-digit"
    });
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col sm:flex-row items-start sm:items-center justify-between gap-4 p-4 rounded-lg border border-border/50 bg-muted/10", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Label, { className: "text-sm font-medium whitespace-nowrap", children: [
        COPY.market,
        ":"
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Button,
          {
            variant: market === "futures" ? "default" : "outline",
            size: "sm",
            onClick: () => onMarketChange("futures"),
            className: market === "futures" ? "bg-neon-yellow/20 text-neon-yellow border-neon-yellow/50 hover:bg-neon-yellow/30" : "",
            children: COPY.marketFutures
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Button,
          {
            variant: market === "spot" ? "default" : "outline",
            size: "sm",
            onClick: () => onMarketChange("spot"),
            className: market === "spot" ? "bg-neon-cyan/20 text-neon-cyan border-neon-cyan/50 hover:bg-neon-cyan/30" : "",
            children: COPY.marketSpot
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 flex-wrap", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Switch,
          {
            checked: enabled,
            onCheckedChange: onEnabledChange,
            id: "polling-toggle"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { htmlFor: "polling-toggle", className: "text-sm whitespace-nowrap", children: enabled ? COPY.pollingActive : COPY.pollingPaused })
      ] }),
      lastUpdated && /* @__PURE__ */ jsxRuntimeExports.jsxs(Badge, { variant: "outline", className: "text-xs whitespace-nowrap", children: [
        COPY.lastUpdated,
        ": ",
        formatTime(lastUpdated)
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Button,
        {
          variant: "outline",
          size: "sm",
          onClick: onRefresh,
          disabled: isLoading,
          className: "border-neon-green/30 text-neon-green hover:bg-neon-green/10",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              RefreshCw,
              {
                className: `w-4 h-4 mr-2 ${isLoading ? "animate-spin" : ""}`
              }
            ),
            COPY.refresh
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Button,
        {
          variant: "outline",
          size: "sm",
          onClick: onToggleSettings,
          className: "border-neon-cyan/30 text-neon-cyan hover:bg-neon-cyan/10",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Settings, { className: "w-4 h-4 mr-2" }),
            COPY.settings
          ]
        }
      )
    ] })
  ] });
}
function OrderFlowStatGrid({
  stats,
  columns = 3
}) {
  const gridClass = {
    2: "grid-cols-1 sm:grid-cols-2",
    3: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3",
    4: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"
  }[columns];
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `grid ${gridClass} gap-4`, children: stats.map((stat) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: `p-4 rounded-lg border border-border/50 bg-muted/20 ${stat.className || ""}`,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between mb-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-medium text-muted-foreground uppercase tracking-wide", children: stat.label }),
          stat.icon
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xl font-bold", children: stat.value })
      ]
    },
    stat.label
  )) });
}
function OrderFlowMonitor() {
  const { identity, isInitializing } = useInternetIdentity();
  const isAuthenticated = !!identity;
  const [market, setMarket] = reactExports.useState("futures");
  const [pollingInterval, setPollingInterval] = reactExports.useState(3e3);
  const [enabled, setEnabled] = reactExports.useState(true);
  const [flowThresholds, setFlowThresholds] = reactExports.useState(
    () => {
      const saved = localStorage.getItem("orderFlowThresholds");
      const parsed = safeJsonParse(saved, null);
      return validateOrderFlowThresholds(parsed);
    }
  );
  const [confluenceThresholds, setConfluenceThresholds] = reactExports.useState(() => {
    const saved = localStorage.getItem("confluenceThresholds");
    const parsed = safeJsonParse(saved, null);
    return validateConfluenceThresholds(parsed);
  });
  const [alertThresholds, _setAlertThresholds] = reactExports.useState(
    () => {
      const saved = localStorage.getItem("alertThresholds");
      const parsed = safeJsonParse(saved, null);
      return validateAlertThresholds(parsed);
    }
  );
  const [confluenceEvents, setConfluenceEvents] = reactExports.useState(
    []
  );
  const [alerts, setAlerts] = reactExports.useState([]);
  const memory = useOrderFlowStableMemory();
  const prevConfluenceRef = reactExports.useRef([]);
  const prevAlertsRef = reactExports.useRef([]);
  const [showSettings, setShowSettings] = reactExports.useState(false);
  const { data, isLoading, error, lastUpdated, refetch } = useBinanceOrderFlow({
    market,
    pollingInterval,
    enabled: enabled && isAuthenticated
  });
  reactExports.useEffect(() => {
    memory.reset();
    setConfluenceEvents([]);
    setAlerts([]);
    prevConfluenceRef.current = [];
    prevAlertsRef.current = [];
  }, [market]);
  reactExports.useEffect(() => {
    try {
      localStorage.setItem(
        "orderFlowThresholds",
        JSON.stringify(flowThresholds)
      );
    } catch (_e) {
      console.warn("Failed to save orderFlowThresholds to localStorage");
    }
  }, [flowThresholds]);
  reactExports.useEffect(() => {
    try {
      localStorage.setItem(
        "confluenceThresholds",
        JSON.stringify(confluenceThresholds)
      );
    } catch (_e) {
      console.warn("Failed to save confluenceThresholds to localStorage");
    }
  }, [confluenceThresholds]);
  reactExports.useEffect(() => {
    try {
      localStorage.setItem("alertThresholds", JSON.stringify(alertThresholds));
    } catch (_e) {
      console.warn("Failed to save alertThresholds to localStorage");
    }
  }, [alertThresholds]);
  const analysis = reactExports.useMemo(() => {
    if (!data) return null;
    if (!Array.isArray(data.recentTrades) || data.recentTrades.length === 0) {
      return null;
    }
    const bookTicker = data.bookTicker && typeof data.bookTicker.bidPrice === "string" && typeof data.bookTicker.askPrice === "string" ? data.bookTicker : null;
    try {
      const classifications = classifyTrades(data.recentTrades, flowThresholds);
      const stats = calculateRollingStats(classifications, flowThresholds);
      const hasCluster = detectLargeTradeCluster(classifications);
      const currentSpread = calculateSpreadMetrics(bookTicker);
      const direction = determineBookDirection(
        currentSpread,
        memory.previousSpread,
        0.01
      );
      return {
        classifications,
        stats,
        hasCluster,
        currentSpread,
        direction
      };
    } catch (err) {
      console.error("Error analyzing order flow:", err);
      return null;
    }
  }, [data, flowThresholds]);
  reactExports.useEffect(() => {
    if (!analysis || !analysis.currentSpread) return;
    try {
      const newEvents = detectConfluenceEvents(
        analysis.stats,
        analysis.currentSpread,
        memory.previousSpread,
        analysis.direction,
        confluenceThresholds,
        prevConfluenceRef.current
      );
      if (JSON.stringify(newEvents) !== JSON.stringify(prevConfluenceRef.current)) {
        setConfluenceEvents(newEvents);
        prevConfluenceRef.current = newEvents;
      }
      memory.setPreviousSpread(analysis.currentSpread);
      const currentPrice = analysis.currentSpread.midPrice;
      const currentVolume = analysis.stats.totalBuyNotional + analysis.stats.totalSellNotional;
      memory.updateAvgVolume(currentVolume);
      memory.updateAvgSpread(analysis.currentSpread.spreadPercent);
      const newAlerts = [];
      const liquidationAlert = generateProxyLiquidationAlert(
        analysis.stats,
        memory.previousStats,
        currentPrice,
        memory.previousPrice,
        alertThresholds
      );
      if (liquidationAlert) newAlerts.push(liquidationAlert);
      const volumeAlert = generateVolumeSpikeAlert(
        analysis.stats,
        memory.avgVolume,
        alertThresholds
      );
      if (volumeAlert) newAlerts.push(volumeAlert);
      const spreadAlert = generateSpreadAnomalyAlert(
        analysis.currentSpread,
        memory.avgSpread,
        alertThresholds
      );
      if (spreadAlert) newAlerts.push(spreadAlert);
      if (newAlerts.length > 0) {
        setAlerts((prev) => {
          const updated = [...newAlerts, ...prev].slice(0, 20);
          prevAlertsRef.current = updated;
          return updated;
        });
      }
      memory.setPreviousStats(analysis.stats);
      memory.setPreviousPrice(currentPrice);
    } catch (err) {
      console.error("Error processing order flow analysis:", err);
    }
  }, [analysis, confluenceThresholds, alertThresholds]);
  if (isInitializing) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center justify-center min-h-[400px]", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center space-y-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-12 h-12 border-4 border-neon-cyan/30 border-t-neon-cyan rounded-full animate-spin mx-auto" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground", children: COPY.loading })
    ] }) });
  }
  if (!isAuthenticated) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center justify-center min-h-[400px]", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-md w-full space-y-6 text-center", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-20 h-20 rounded-full bg-neon-yellow/10 border-2 border-neon-yellow/30 flex items-center justify-center mx-auto", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Lock, { className: "w-10 h-10 text-neon-yellow" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-2xl font-bold text-neon-yellow", children: COPY.authRequired }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground", children: COPY.authMessage })
      ] })
    ] }) });
  }
  const formatCurrency = (value) => {
    return new Intl.NumberFormat("en-US", {
      style: "currency",
      currency: "USD",
      minimumFractionDigits: 0,
      maximumFractionDigits: 0
    }).format(value);
  };
  const calculateImbalancePercent = (stats) => {
    const totalFlow = stats.totalBuyNotional + stats.totalSellNotional;
    if (totalFlow === 0) return 0;
    return stats.netDelta / totalFlow * 100;
  };
  if (!data && !isLoading && !error) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center justify-center min-h-[400px]", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-md w-full space-y-6 text-center", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-20 h-20 rounded-full bg-muted/10 border-2 border-muted/30 flex items-center justify-center mx-auto", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Database, { className: "w-10 h-10 text-muted-foreground" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-2xl font-bold", children: COPY.waitingForData }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground", children: COPY.waitingMessage })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        Button,
        {
          onClick: () => setEnabled(true),
          className: "bg-neon-cyan/20 text-neon-cyan hover:bg-neon-cyan/30 border border-neon-cyan/50",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Activity, { className: "w-4 h-4 mr-2" }),
            COPY.enablePolling
          ]
        }
      )
    ] }) });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-6", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center justify-between", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-2xl font-bold text-neon-yellow", children: COPY.title }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-sm text-muted-foreground", children: [
        COPY.subtitle,
        " -",
        " ",
        market === "futures" ? COPY.marketFutures : COPY.marketSpot
      ] })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      OrderFlowControlsBar,
      {
        market,
        onMarketChange: setMarket,
        enabled,
        onEnabledChange: setEnabled,
        lastUpdated,
        isLoading,
        onRefresh: refetch,
        showSettings,
        onToggleSettings: () => setShowSettings(!showSettings)
      }
    ),
    showSettings && /* @__PURE__ */ jsxRuntimeExports.jsx(
      OrderFlowSection,
      {
        title: COPY.analysisSettings,
        icon: Activity,
        className: "border-neon-cyan/30",
        children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-3 gap-4", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs", children: COPY.largeTradeThreshold }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Input,
                {
                  type: "number",
                  value: flowThresholds.largeTradeNotional,
                  onChange: (e) => setFlowThresholds((prev) => ({
                    ...prev,
                    largeTradeNotional: Number(e.target.value)
                  })),
                  className: "terminal-input"
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs", children: COPY.tradeWindow }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Input,
                {
                  type: "number",
                  value: flowThresholds.rollingWindowTrades,
                  onChange: (e) => setFlowThresholds((prev) => ({
                    ...prev,
                    rollingWindowTrades: Number(e.target.value)
                  })),
                  className: "terminal-input"
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs", children: COPY.timeWindow }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Input,
                {
                  type: "number",
                  value: flowThresholds.rollingWindowMinutes,
                  onChange: (e) => setFlowThresholds((prev) => ({
                    ...prev,
                    rollingWindowMinutes: Number(e.target.value)
                  })),
                  className: "terminal-input"
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(Separator, {}),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "grid grid-cols-1 md:grid-cols-3 gap-4", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs", children: COPY.minImbalance }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Input,
                {
                  type: "number",
                  value: confluenceThresholds.minImbalancePercent,
                  onChange: (e) => setConfluenceThresholds((prev) => ({
                    ...prev,
                    minImbalancePercent: Number(e.target.value)
                  })),
                  className: "terminal-input"
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs", children: COPY.minSpreadChange }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Input,
                {
                  type: "number",
                  value: confluenceThresholds.minSpreadChangePercent,
                  onChange: (e) => setConfluenceThresholds((prev) => ({
                    ...prev,
                    minSpreadChangePercent: Number(e.target.value)
                  })),
                  className: "terminal-input"
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Label, { className: "text-xs", children: COPY.pollingInterval }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Input,
                {
                  type: "number",
                  value: pollingInterval,
                  onChange: (e) => setPollingInterval(Math.max(1e3, Number(e.target.value))),
                  className: "terminal-input",
                  min: 1e3,
                  step: 1e3
                }
              )
            ] })
          ] })
        ] })
      }
    ),
    error && /* @__PURE__ */ jsxRuntimeExports.jsxs(Alert, { variant: "destructive", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(TriangleAlert, { className: "h-4 w-4" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(AlertDescription, { children: error })
    ] }),
    data && analysis && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(OrderFlowSection, { title: COPY.overview, icon: ChartColumn, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        OrderFlowStatGrid,
        {
          stats: [
            {
              label: COPY.currentPrice,
              value: data.ticker ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-neon-cyan", children: [
                "$",
                Number.parseFloat(data.ticker.lastPrice).toFixed(2)
              ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "--" })
            },
            {
              label: COPY.spread,
              value: analysis.currentSpread ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-neon-yellow", children: [
                analysis.currentSpread.spreadPercent.toFixed(3),
                "%"
              ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "--" })
            },
            {
              label: COPY.volume24h,
              value: data.ticker ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-neon-green", children: formatCurrency(
                Number.parseFloat(data.ticker.quoteVolume)
              ) }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "--" })
            }
          ]
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(OrderFlowSection, { title: COPY.flowImbalance, icon: Activity, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          OrderFlowStatGrid,
          {
            columns: 4,
            stats: [
              {
                label: COPY.buyFlow,
                value: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-neon-green", children: formatCurrency(analysis.stats.totalBuyNotional) }),
                icon: /* @__PURE__ */ jsxRuntimeExports.jsx(TrendingUp, { className: "w-4 h-4 text-neon-green" })
              },
              {
                label: COPY.sellFlow,
                value: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-neon-pink", children: formatCurrency(analysis.stats.totalSellNotional) }),
                icon: /* @__PURE__ */ jsxRuntimeExports.jsx(TrendingDown, { className: "w-4 h-4 text-neon-pink" })
              },
              {
                label: COPY.netDelta,
                value: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "span",
                  {
                    className: analysis.stats.netDelta > 0 ? "text-neon-green" : analysis.stats.netDelta < 0 ? "text-neon-pink" : "text-muted-foreground",
                    children: formatCurrency(analysis.stats.netDelta)
                  }
                )
              },
              {
                label: COPY.imbalance,
                value: /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "span",
                  {
                    className: calculateImbalancePercent(analysis.stats) > 0 ? "text-neon-green" : calculateImbalancePercent(analysis.stats) < 0 ? "text-neon-pink" : "text-muted-foreground",
                    children: [
                      calculateImbalancePercent(analysis.stats).toFixed(1),
                      "%"
                    ]
                  }
                )
              }
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-4 grid grid-cols-1 sm:grid-cols-2 gap-4", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 rounded-lg border border-border/50 bg-muted/20", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between mb-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-medium text-muted-foreground uppercase tracking-wide", children: COPY.largeOrders }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Zap, { className: "w-4 h-4 text-neon-yellow" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xl font-bold", children: analysis.stats.largeTradeCount })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 rounded-lg border border-border/50 bg-muted/20", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center justify-between mb-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-medium text-muted-foreground uppercase tracking-wide", children: COPY.clusterDetected }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xl font-bold", children: analysis.hasCluster ? /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-neon-pink", children: COPY.clusterDetected }) : /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: COPY.noCluster }) })
          ] })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(ConfluencePanel, { events: confluenceEvents }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(AlertsPanel, { alerts, onClearAlerts: () => setAlerts([]) })
    ] })
  ] });
}
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      refetchOnWindowFocus: false,
      retry: 1,
      staleTime: 3e4
    }
  }
});
function FuturesMonitorTabSurface() {
  const [resetKey, setResetKey] = reactExports.useState(0);
  const handleReset = () => {
    setResetKey((prev) => prev + 1);
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsx(QueryClientProvider, { client: queryClient, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(J, { attribute: "class", defaultTheme: "dark", enableSystem: true, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-h-screen bg-background text-foreground flex flex-col", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("header", { className: "border-b border-border/40 bg-card/50 backdrop-blur supports-[backdrop-filter]:bg-card/50", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "container mx-auto px-4 py-4 flex items-center justify-between", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Activity, { className: "w-6 h-6 text-neon-cyan" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { className: "text-xl font-bold text-neon-cyan", children: "Order Flow Monitor" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(LoginButton, {})
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: "flex-1 container mx-auto px-4 py-8", children: /* @__PURE__ */ jsxRuntimeExports.jsx(OrderFlowErrorBoundary, { onReset: handleReset, children: /* @__PURE__ */ jsxRuntimeExports.jsx(OrderFlowMonitor, {}, resetKey) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Footer, {})
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Toaster, {})
  ] }) });
}
export {
  FuturesMonitorTabSurface as default
};
